#include <tunables/global>

profile wonderful_wino flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  
  /app/** r,
  
  /share/wwino/** rwk,
  
  network,
  network bind 5000,
  
  deny /**,
}